#ifndef SETTINGS_WINDOW_H
#define SETTINGS_WINDOW_H

#include "app.h"

/* Open (or focus, if already open) the settings + calendar window. */
void settings_window_open(App *app);

#endif /* SETTINGS_WINDOW_H */
